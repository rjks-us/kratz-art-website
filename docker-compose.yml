version: '3.8'

services:
  kratz-art:
    build:
      context: ./app
      dockerfile: Dockerfile
    ports:
      - "4000:3000"
    restart: unless-stopped
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production